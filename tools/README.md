# tools

This folder is intentionally empty.

If PlatformIO cannot find `variant = t_echo_lite_nrf52840`, copy LilyGO's original variant and bootloader folders into your PlatformIO installation, or run LilyGO's original setup script from their repository:

`tools/win10 vscode platformio start/t-echo-lite_setup.py`
